import React from 'react';

const Header = () => {
  return (
    <header style={{ padding: '1rem', borderBottom: '1px solid #444' }}>
      <h1 style={{ fontSize: '1.5rem', color: 'var(--neon-green)' }}>Ambarish() Portfolio</h1>
    </header>
  );
};

export default Header;
